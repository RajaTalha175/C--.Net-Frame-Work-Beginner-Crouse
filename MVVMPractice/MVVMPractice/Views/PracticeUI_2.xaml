<Window x:Class="MVVMPractice.Views.PracticeUI_2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:viewmodels="clr-namespace:MVVMPractice.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:SVMUsingManual}"
        mc:Ignorable="d"
        Title="PracticeUI_2"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <StackPanel>

            <TextBlock Text="Student Name" FontWeight="Bold"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Email" FontWeight="Bold"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Address" FontWeight="Bold"/>
            <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

            <TextBlock Text="Password" FontWeight="Bold"/>
            <Grid Margin="0,5,0,10">
                <PasswordBox x:Name="PasswordBox" Visibility="Visible" Width="300" />
                <TextBox x:Name="VisiblePasswordBox" Visibility="Collapsed" Width="300" />
                <Button x:Name="ShowHideButton"
                        Content="👁"
                        Width="40" Height="25"
                        HorizontalAlignment="Right"
                        Click="ShowHideButton_Click"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,10">
                <Button Content="Add / Update" Width="100" Margin="5" Click="AddOrUpdate_Click"/>
                <Button Content="Delete" Width="100" Margin="5" Click="Delete_Click"/>
                <Button Content="Clear" Width="100" Margin="5" Click="Clear_Click"/>
            </StackPanel>

            <ListView ItemsSource="{Binding Students}"
                      SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                      Margin="0,20,0,0"
                      Height="200">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="120"/>
                        <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}" Width="150"/>
                        <GridViewColumn Header="Address" DisplayMemberBinding="{Binding Address}" Width="180"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </StackPanel>
    </Grid>
</Window>
